# pnpm + VitePress(mermaid) 兼容：
# vitepress-plugin-mermaid 会把 dayjs / cytoscape 等 mermaid 的传递依赖
# 写进 Vite 的 optimizeDeps.include。但 pnpm 默认不把这些嵌套依赖提升到根
# node_modules，导致 Vite 解析失败、dev 端报
# "does not provide an export named 'default'" 而白屏。
# 把它们提升到根，使 Vite 能正常预打包这些依赖。
public-hoist-pattern[]=*dayjs*
public-hoist-pattern[]=*@braintree/sanitize-url*
public-hoist-pattern[]=*debug*
public-hoist-pattern[]=*cytoscape*
